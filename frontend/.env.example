# In development, leave this unset — Vite proxy handles /api → localhost:5000
# In production (Vercel), set this to your Render backend URL:
# VITE_API_URL=https://your-backend.onrender.com/api
VITE_API_URL=
